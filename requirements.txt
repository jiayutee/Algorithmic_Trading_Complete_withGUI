# UI — desktop (PyQt5) and web (Dash)
PyQt5
PyQtWebEngine
streamlit
dash>=2.16,<3
dash-bootstrap-components>=1.5,<2

# Data and ML
pandas
numpy
scikit-learn
tensorflow
torch
transformers
stable-baselines3
gym
joblib

# Finance and Trading
# FinRL is often installed from github:
# pip install git+https://github.com/AI4Finance-Foundation/FinRL.git
finrl
yfinance<0.2.60
backtrader
python-binance
ccxt
alpaca-py
alpaca-trade-api
ib-insync
ta
pandas-ta
PyPortfolioOpt
# OpenBB Platform (unified data provider) — requires Python >=3.9,<3.13
# NOTE: yfinance is pinned <0.2.60 above — newer yfinance releases require a
# curl_cffi session internally, but openbb-yfinance==1.4.2 still passes a
# plain requests.Session, which raises YFDataException on every call.
# Verified working combo: yfinance==0.2.55 + openbb-yfinance==1.4.2
# (Day 5 T6f validation).
openbb
openbb-yfinance

# Web and API
requests
websockets
websocket-client
openai

# Utilities
beautifulsoup4
matplotlib
plotly
scipy
statsmodels
python-dotenv

# Testing
pytest